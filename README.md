# PartnersApp - Система управления партнерами

## Описание
WPF приложение для управления партнерами компании с функциями авторизации, CRUD операций и экспорта данных. Проект создан для подготовки к экзамену по разработке десктопных приложений.

## Функциональность

### 🔐 Авторизация
- Вход в систему с логином и паролем
- Два уровня доступа: Администратор и Пользователь
- Смена пользователя без перезапуска приложения
- Тестовые учетные записи:
  - `admin` / `admin123` (Администратор)
  - `user` / `user123` (Пользователь)

### 👥 Управление партнерами
- **Просмотр** списка всех партнеров в таблице
- **Добавление** новых партнеров (только для администраторов)
- **Редактирование** данных партнеров (только для администраторов)
- **Удаление** партнеров с каскадным удалением связанных записей (только для администраторов)

### 📊 История продаж
- Просмотр истории продаж для каждого партнера
- Добавление новых продаж (только для администраторов)
- Расчет общей суммы продаж и скидок

### 📁 Экспорт данных
- Экспорт списка партнеров в CSV формат
- Корректная кодировка UTF-8 для русского языка
- Автоматическое имя файла с текущей датой

### 🎨 Интерфейс
- Темная тема оформления
- Статус-бар с информацией о текущем пользователе
- Отображение текущего времени по МСК (UTC+3)
- Визуальное отключение кнопок для обычных пользователей
- Центрирование окон при запуске

## Технические особенности

### Архитектура
- **WPF** с XAML разметкой
- **Entity Framework 5.0** для работы с базой данных
- **MVVM** паттерн с привязкой данных
- **SQL Server** база данных

### База данных
- Таблицы: Partners, PartnerTypes, Products, SalesHistory, Users
- Связи с внешними ключами
- Каскадное удаление для целостности данных

### Безопасность
- Проверка прав доступа на уровне UI и бизнес-логики
- Валидация входных данных
- Обработка ошибок подключения к БД

## Структура проекта
```
PartnersApp/
├── dbContext/          # Entity Framework модели
├── Models/             # Бизнес-модели
├── Services/           # Сервисы для работы с данными
├── Windows/            # XAML окна приложения
└── App.xaml           # Точка входа приложения
```

## Требования
- .NET Framework 4.5+
- SQL Server (LocalDB или полная версия)
- Visual Studio 2019+

## Настройка
Настроить строку подключения в App.config
