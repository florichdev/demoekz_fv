<Window x:Class="PartnersApp.ProductWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактирование продукта" Height="260" Width="400" Background="#FF424242">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Наименование:" Foreground="White"/>
        <TextBox Grid.Row="1" Text="{Binding Product.ProductName}" Margin="0,5,0,10"/>

        <TextBlock Grid.Row="2" Text="Цена:" Foreground="White"/>
        <TextBox Grid.Row="3" Text="{Binding Product.Price}" Margin="0,5,0,10"/>

        <StackPanel Grid.Row="4" HorizontalAlignment="Center" Margin="0,0,0,0" Orientation="Horizontal">
            <Button Content="Сохранить" Width="80" Margin="0,0,10,0" Click="SaveButton_Click" Background="#FF383838" Foreground="White"/>
            <Button Content="Отмена" Width="80" Click="CancelButton_Click" Foreground="White" Background="#FF383838"/>
        </StackPanel>
    </Grid>
</Window>