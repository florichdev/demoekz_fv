<Window x:Class="PartnersApp.AddSaleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Добавить продажу" Height="280" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize" Background="#FF424242">
    <Grid Margin="10" Background="#FF424242">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Продукт:" Margin="0,0,0,5" Foreground="White"/>
        <ComboBox Grid.Row="1" x:Name="ProductComboBox" DisplayMemberPath="ProductName"
                  SelectedValuePath="ProductId" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="2" Text="Количество:" Margin="0,0,0,5" Foreground="White"/>
        <TextBox Grid.Row="3" x:Name="QuantityTextBox" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Добавить" Width="100" Margin="0,0,10,0" Click="SaveButton_Click" Foreground="White" Background="#FF383838"/>
            <Button Content="Отмена" Width="100" Margin="0,0,10,0" Click="CancelButton_Click" Foreground="White" Background="#FF383838"/>
        </StackPanel>
    </Grid>
</Window>